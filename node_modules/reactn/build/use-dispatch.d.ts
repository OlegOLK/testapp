import { Reducers, State } from '../default';
import GlobalStateManager from './global-state-manager';
import Reducer, { Dispatcher, ExtractArguments } from './typings/reducer';
export declare type UseDispatch<G extends {} = State, R extends {} = Reducers, K extends keyof R = keyof R, A extends any[] = any[]> = Dispatcher<G, A> | Dispatcher<G, ExtractArguments<R[K]>>;
export default function useDispatch<G extends {} = State, R extends {} = Reducers, A extends any[] = any[]>(overrideGlobalStateManager: GlobalStateManager<G, any> | null, reducer: Reducer<G, R, A>): Dispatcher<G, A>;
export default function useDispatch<G extends {} = State, R extends {} = Reducers, K extends keyof R = keyof R>(overrideGlobalStateManager: GlobalStateManager<G, R> | null, reducer: K): Dispatcher<G, ExtractArguments<R[K]>>;
